const ProductInfo = ({ data }) => {
  return (
    <div>
      <div>
        <ul>
          {/* {data.map((product) => {
            return (
              <li>
                <button>{product.name}</button>
              </li>
            );
          })} */}
        </ul>
      </div>
      <p></p>
    </div>
  );
};

export default ProductInfo;
